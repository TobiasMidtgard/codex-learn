# Imported Claude Design source

Pulled from https://claude.ai/design/p/9a2d3378-651a-4f72-8ae0-96379a067b1b
via the DesignSync MCP. Kept for reference — neither file ships in the build.

  Codex Learn.dc.html   the design: five screens (dashboard, tracks, lesson,
                        challenge, profile) as an <x-dc> template
  support.js            Claude Design's dc-runtime that renders that template

The design language it defines is implemented in src/index.head.html (tokens,
components, motion) and src/app.js (icon rail, header, dashboard, progress).
See the README's "Design" section.
